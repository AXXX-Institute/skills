interface:
  display_name: "Adversarial Review"
  short_description: "Independent fresh-context code review"
  default_prompt: "Use $adversarial-review to independently review my freshly committed change."
policy:
  allow_implicit_invocation: false
